var showBannerCB;
function handlevk(keyCode) {
  var KeyEvent = window["KeyEvent"] || {};
  KeyEvent.VK_LEFT = KeyEvent.VK_LEFT || window["VK_LEFT"] || 37;
  KeyEvent.VK_RIGHT = KeyEvent.VK_RIGHT || window["VK_RIGHT"] || 39;
  KeyEvent.VK_ENTER = KeyEvent.VK_ENTER || window["VK_ENTER"] || 13;
  switch (keyCode) {
    case KeyEvent.VK_ENTER:
      var hasConsent = isConsentOptInButtonSelected();
      hideBanner();
      __tcfapi("setConsent", 2, showBannerCB, hasConsent);
      break;
    case KeyEvent.VK_LEFT:
    case KeyEvent.VK_RIGHT:
      toggleConsentButtons();
      break;
    default:
      break;
  }
}
function kbd(cb) {
  showBannerCB = cb;
}
